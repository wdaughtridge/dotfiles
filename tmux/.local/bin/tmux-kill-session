#!/usr/bin/env bash

if [[ $# -eq 1 ]]; then
    selected=$1
else
    selected=$(tmux list-sessions | fzf)
fi

selected_name=$(echo "$selected" | cut -d' ' -f1)

tmux kill-session -a -t"$selected_name"
